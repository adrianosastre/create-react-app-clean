const WelcomePage = (props) => {
    return (
        <section>
            <h2>Welcome!</h2>
            <p>This is a clean react app with react-router-dom installed.</p>
        </section>
    );
};

export default WelcomePage;
